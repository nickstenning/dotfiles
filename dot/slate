config defaultToCurrentScreen true
config nudgePercentOf screenSize
config resizePercentOf screenSize

# Monitor aliases
alias mon-laptop 1280x800
alias mon-dell   2560x1440

# Rows and columns
alias col screenSizeX/12
alias row screenSizeY/12

# Position Aliases
alias 1-left  move screenOriginX;screenOriginY                   10*${col};12*${row}
alias 1-full  move screenOriginX;screenOriginY                   12*${col};12*${row}
alias 2-left  move screenOriginX;screenOriginY                   8*${col};12*${row}
alias 2-right move screenOriginX+8*${col};screenOriginY          4*${col};12*${row}
alias 3-left  move screenOriginX;screenOriginY                   5*${col};12*${row}
alias 3-right move screenOriginX+5*${col};screenOriginY          7*${col};12*${row}
alias w-top   move screenOriginX;screenOriginY                   12*${col};6*${row}
alias w-bot   move screenOriginX;screenOriginY+6*${row}          12*${col};6*${row}

# Layouts
layout 1col 'Google Chrome':REPEAT    ${1-left}
layout 1col 'iTerm':REPEAT            ${1-full}
layout 2col 'Google Chrome':REPEAT    ${2-left}
layout 2col 'iTerm':REPEAT            ${2-right}
layout 3col 'Google Chrome':REPEAT    ${3-left}
layout 3col 'iTerm':REPEAT            ${3-right}
layout wide 'Google Chrome':REPEAT    ${w-top}
layout wide 'iTerm':REPEAT            ${w-bot}

# Default layouts
default 1col resolutions:${mon-laptop}
default 2col resolutions:${mon-dell}

# Focus Bindings
bind l:cmd,ctrl       focus right
bind h:cmd,ctrl       focus left
bind k:cmd,ctrl       focus up
bind j:cmd,ctrl       focus down
bind k:shift,cmd,ctrl focus behind
bind j:shift,cmd,ctrl focus behind
bind f3 focus 'Google Chrome'
bind f4 focus 'iTerm'
bind f5 focus 'MailMate'

# Modal mappings
bind r:f2 relaunch
bind 1:f2 layout 1col
bind 2:f2 layout 2col
bind 3:f2 layout 3col
bind 9:f2 layout wide
bind .:f2 grid
bind ,:f2 hint
bind m:f2 corner top-left resize:screenSizeX;screenSizeY
bind h:f2 corner top-left resize:screenSizeX/2;screenSizeY
bind l:f2 corner top-right resize:screenSizeX/2;screenSizeY
bind j:f2 corner bottom-left resize:screenSizeX;screenSizeY/2
bind k:f2 corner top-left resize:screenSizeX;screenSizeY/2
