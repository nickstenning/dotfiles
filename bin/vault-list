#!/bin/sh

set -eu

cd "$VAULTSDIR"
for f in *.sparsebundle; do
  name=${f%%.sparsebundle}
  printf "$name" 
  if vault-loc "$name" >/dev/null 2>&1; then
    printf "\t$(vault-loc "$name")"  
  fi
  printf "\n"
done
