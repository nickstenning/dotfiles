#!/bin/sh

set -eu

cd "$VAULTSDIR"

NAME=$1

if ! vault-loc "$NAME" 2>/dev/null; then
  echo "not mounted" >&2
  exit 1
fi

hdiutil detach "$(vault-loc "$NAME")" >/dev/null
rm "${NAME}.$(hostname -s).mount"

