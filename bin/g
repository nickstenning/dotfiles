#!/bin/sh

#
# A wrapper around mvim that attempts to do the right thing when given
# arguments.
#

set -eu

if [ $# -gt 0 ]; then
  if echo "$@" | egrep -q '(^|\s)--?\S+'; then
    exec mvim "$@"
  else
    exec mvim --remote-silent "$@"
  fi
else
  exec mvim
fi
