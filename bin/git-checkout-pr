#!/bin/sh

usage () {
  echo "Usage: $(basename "$0") <pull request number>"
}

main () {
  if [ $# -ne 1 ]; then
    usage >&2
    exit 1
  fi

  git fetch origin "refs/pull/$1/head:refs/remotes/origin-pr/$1"
  git checkout -tb "pr/$1" "origin-pr/$1"
}

main "$@"

